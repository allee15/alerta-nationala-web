export const poppins = {
  regular: (size: number) => ({
    fontFamily: 'Poppins',
    fontWeight: 400,
    fontSize: `${size}px`,
  }),
  semiBold: (size: number) => ({
    fontFamily: 'Poppins',
    fontWeight: 600,
    fontSize: `${size}px`,
  }),
  bold: (size: number) => ({
    fontFamily: 'Poppins',
    fontWeight: 700,
    fontSize: `${size}px`,
  }),
};